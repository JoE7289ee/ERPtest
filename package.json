{
  "name": "erptest",
  "version": "1.0.0",
  "private": true,
  "description": "ERP end-to-end tests with Playwright and Docker",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "1.53.1",
    "@types/node": "24.0.10"
  }
}
